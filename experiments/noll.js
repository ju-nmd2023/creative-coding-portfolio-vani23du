const squareSize = 10;
const squareGap = 5;
const squareAmount = 20;

function setup() {
  createCanvas(innerHeight, innerHeight);
  background(255, 255, 255);
}

function draw() {
  stroke(0, 0, 0);
  noFill();
}
